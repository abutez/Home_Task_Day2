#prints the maximum and minimum of a list
def MaxMin(mylist):
    #result should be in an array
    mylist=[]
    #if minimum and maximum is equal returns the number
    if max in (mylist)==min(mylist):
        return len(mylist)
    #returns the minimum and the maximum
    else:
        return [min(mylist), max(mylist)]



